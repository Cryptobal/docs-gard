'use client';

import { AppTopbar } from './AppTopbar';

/**
 * DocumentosTopbar - Topbar para páginas de Documentos / Gestión Documental
 * Sin acciones extra a la derecha (Templates se accede desde Gestión Documental).
 */
export function DocumentosTopbar() {
  return <AppTopbar />;
}
